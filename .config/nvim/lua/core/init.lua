local M = {
  tmux = require("core.tmux"),
  code = require("core.code"),
  windows = require("core.windows"),
  jumplist = require("core.jumplist"),
  registers = require("core.registers"),
}

return M
